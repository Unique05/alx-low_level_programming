#include <stdio.h>

/**
 * main - prints digits
 * Description: Prints all single
 * numbers in base ten
 * Return: 0
 */

int main(void)
{
	int c;

	for (c = 0; c < 10; c++)
		printf("%d", c);
	printf("\n");
	return (0);
}
